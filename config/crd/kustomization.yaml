# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/ovirt.cluster.k8s.io_ovirtclusterproviderspecs.yaml
- bases/ovirt.cluster.k8s.io_ovirtclusterproviderstatuses.yaml
- bases/ovirt.cluster.k8s.io_ovirtmachineproviderspecs.yaml
- bases/ovirt.cluster.k8s.io_ovirtmachineproviderstatuses.yaml
# +kubebuilder:scaffold:crdkustomizeresource

patches:
# [WEBHOOK] patches here are for enabling the conversion webhook for each CRD
#- patches/webhook_in_ovirtclusterproviderspecs.yaml
#- patches/webhook_in_ovirtclusterproviderstatuses.yaml
#- patches/webhook_in_ovirtmachineproviderspecs.yaml
#- patches/webhook_in_ovirtmachineproviderstatuses.yaml
# +kubebuilder:scaffold:crdkustomizewebhookpatch

# [CAINJECTION] patches here are for enabling the CA injection for each CRD
#- patches/cainjection_in_ovirtclusterproviderspecs.yaml
#- patches/cainjection_in_ovirtclusterproviderstatuses.yaml
#- patches/cainjection_in_ovirtmachineproviderspecs.yaml
#- patches/cainjection_in_ovirtmachineproviderstatuses.yaml
# +kubebuilder:scaffold:crdkustomizecainjectionpatch

# the following config is for teaching kustomize how to do kustomization for CRDs.
configurations:
- kustomizeconfig.yaml
